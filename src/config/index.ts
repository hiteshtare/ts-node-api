export const APP_CONFIG = {
  version: "api_version",
  loggerLevel: 'debug',
  endPointUrl: '', //Assigned via ENV variable
  mongoCollectionName: '', //Assigned via fetchedModeratorConfig
  // mongoDBName: '', //Assigned via Config Map
};
