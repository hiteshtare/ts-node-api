export const APP_CONFIG = {
  version: "api_version",
  loggerLevel: 'debug',
  // mongoDBName: '', //Assigned via Config Map
  // mongoCollectionName: '', //Assigned via Config Map
  endPointUrl: '' //Assigned via ENV variable
};
